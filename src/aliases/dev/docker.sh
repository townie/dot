# docker helpers

alias ddflt="docker-machine start default && eval \$(docker-machine env default)"
alias denv="eval \$(docker-machine env default)"
alias dps="docker ps"
alias dmls="docker-machine ls -a"